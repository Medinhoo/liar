{
  "name": "menteur",
  "version": "1.0.0",
  "description": "Jeu de cartes Menteur multijoueur",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd server && npm install && cd ../client && npm install && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev"
  },
  "keywords": ["game", "cards", "multiplayer", "socketio"],
  "author": "",
  "license": "ISC"
}
